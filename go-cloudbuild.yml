steps:
    - id: "Gerando build da image"
      name: 'gcr.io/cloud-builders/docker'
      args: ['build', '-t', 'gcr.io/$PROJECT_ID/go-sum', '.']
    
    - name: 'gcr.io/$PROJECT_ID/go-sum'
      args: ['version']
images:
    - 'gcr.io/$PROJECT_ID/go-sum:latest'